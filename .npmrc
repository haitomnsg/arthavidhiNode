# NPM Configuration for cPanel deployment
# Optimized for shared hosting with limited resources

# Disable scripts that may cause issues on shared hosting
ignore-scripts=false

# Use less memory
maxsockets=3

# Faster installs
prefer-offline=true
audit=false
fund=false

# Reduce logging
loglevel=error

# Ignore optional dependencies that may fail (like AI packages)
optional=false
